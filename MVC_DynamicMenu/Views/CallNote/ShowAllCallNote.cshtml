@model List<MainCallNoteTb>
@{
    ViewData["Title"] = "Home page";
}
<head>
    <link href="~/css/Addfile.css" rel="stylesheet" />
</head>
<div class="container">
    <header class="header">
        <h1 id="title" class="text-center">Case Note</h1>
        <p id="description" class="description text-center">

        </p>
    </header>
    <div class="text-right">
        <input onkeyup="myFunction()" id="myInput" type="text" name="text" class="search" placeholder="Search here!">
        
        <button type="button" onclick='window.location.href = "/CallNote/AddCallNote"' class="btn btn-primary">+Add Note</button>
    </div>
    <br />

    <form id="survey-form">


        <div class="row">
            <div class="col-md-12">
                <table class="table badge-light" id="myTable">
                    <thead>

                    </thead>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.ParticipantID</td>
                            <td>@item.Call_note</td>
                            <td>
                                <a asp-controller="CallNote" asp-action="UpdateCallNote" asp-route-id="@item.ParticipantID">Update</a>
                                <a asp-controller="CallNote" asp-action="DeleteActivityLog" asp-route-id="@item.ParticipantID">Delete</a>
                            </td>
                        </tr>
                    }
              

                </table>
            </div>
        </div>

        <script type="text/javascript">
            const projectName = 'survey-form';

        </script>
        @section Scripts{
            <script>
                function myFunction() {
                    var input, filter, table, tr, td, i, txtValue;
                    input = document.getElementById("myInput");
                    filter = input.value.toUpperCase();
                    table = document.getElementById("myTable");
                    tr = table.getElementsByTagName("tr");
                    for (i = 0; i < tr.length; i++) {
                        td = tr[i].getElementsByTagName("td")[1];
                        if (td) {
                            txtValue = td.textContent || td.innerText;
                            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                                tr[i].style.display = "";
                            } else {
                                tr[i].style.display = "none";
                            }
                        }
                    }
                }
            </script>


        }
    </form>


</div>


